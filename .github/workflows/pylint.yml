name: pylint

on:
  push:
  pull_request:

jobs:
  start-pylint:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: python3 -m pip install pylint
      - run: pylint deepip
